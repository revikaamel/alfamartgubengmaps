name: alfamart_gubeng_maps
description: "A new Flutter project."

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  google_maps_flutter: ^2.12.3
  geolocator: ^10.1.0
  flutter_polyline_points: ^2.0.0
  http: ^1.2.1
  image_picker: ^1.1.2
  supabase_flutter: ^2.8.4

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
